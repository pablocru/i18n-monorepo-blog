{
  "name": "i18n-monorepo-blog",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/components/*",
    "packages/layouts/*",
    "packages/websites/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @websites/* run dev",
    "build": "pnpm --filter @websites/* run build"
  }
}
